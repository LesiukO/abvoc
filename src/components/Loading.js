import React, { useEffect } from 'react';
import Container from './Container';

const Loading = () => {
	return (
		<Container>
			<div>Loading...</div>
		</Container>
	);
};

export default Loading;
